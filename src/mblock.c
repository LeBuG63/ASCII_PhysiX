#include "mblocks.h"
#include "linkedlist.h"
#include "map.h"
#include "funct.h"

void	piston(linkedlist_t *_ll, point_t *_p, __uint _where, int *_side ,int *_stop)
{
	if(get_char_x(*_p, -1) != _map.cborder && *_side == 1)
	{

	}
}
